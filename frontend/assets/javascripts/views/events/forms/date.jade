.field
  label Event Start
    input(type="datetime-local" ng-model="date.data.start" date-fix to-unix required)
.field
  label Event Finish
    input(type="datetime-local" ng-model="date.data.finish" date-fix to-unix required)